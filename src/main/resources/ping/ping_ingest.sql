USE ${PING_DB};

MSCK REPAIR TABLE RAW SYNC PARTITIONS;
SHOW PARTITIONS RAW;

-- Build/Transfer RAW to Working
FROM
    RAW
INSERT
INTO
    PING
SELECT
    BATCH_ID
  , TIME_STAMP
  , SOURCE_IP
  , SOURCE_HOST
  , TARGET_HOST
  , STATUS
  , LETANCY
WHERE
    BATCH_ID = "${BATCH_ID}";
